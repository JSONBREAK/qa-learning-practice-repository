# 🗂️ 04. Test Data Strategy

**หมวด:** QA Process  
**Objective:** วางแผนระดับ High-level ว่าเราจะเอาข้อมูลมาจากไหน และจะจัดการมันอย่างไร  

**Test Data Strategy** จะเป็นระดับ "วางแผน" เพื่อตอบคำถามว่า **"เราจะเอาข้อมูลมาจากไหน และจะจัดการมันอย่างไร?"** ก่อนที่เราจะไปลงมือออกแบบข้อมูลตัวเลขในไฟล์ [[05. Test Data Design]] 

---

## 🏗️ 1. Test Data Sources (แหล่งที่มาของข้อมูล)

เราควรตกลงกับทีมว่าข้อมูลจะมาจากช่องทางไหน:

- **Manual Creation:** สร้างเองทีละเคสผ่าน UI (เหมาะกับโปรเจกต์เล็ก)
- **Database Script (SQL):** เขียน Script เพื่อ Insert ข้อมูลลง DB โดยตรง (รวดเร็ว, ได้ปริมาณมาก)
- **API Injection:** ใช้ API ในการสร้างข้อมูลตั้งต้น (เช่น สร้าง User ผ่าน API ก่อนเริ่มเทส UI)
- **Data Masking / Scrambling:** นำข้อมูลจาก Production มา "พรางตา" (ตัดชื่อจริง/เบอร์โทรออก) เพื่อใช้ใน Staging (สมจริงแต่ต้องระวังเรื่อง Privacy)

---

## 🛡️ 2. Data Security & Privacy (ความปลอดภัย)

กฎเหล็กในการจัดการข้อมูลเทส:

- **No Real PII:** ห้ามใช้ข้อมูลส่วนบุคคลจริง (Personal Identifiable Information) 
	- เช่น เลขบัตรประชาชนจริง, เบอร์โทรจริง ของลูกค้า
- **Compliance:** ต้องสอดคล้องกับกฎหมายคุ้มครองข้อมูลส่วนบุคคล (เช่น PDPA / GDPR)
- **Environment Isolation:** ข้อมูลเทสควรอยู่ใน Environment ที่แยกจาก Production โดยสิ้นเชิง
    
---

## 🔄 3. Data Lifecycle Management (วงจรชีวิตข้อมูล)

ข้อมูลเทสไม่ได้อยู่ตลอดไป เราต้องจัดการดังนี้:

- **Setup:** วิธีการเตรียมข้อมูลก่อนเริ่มเทส (Pre-condition)
- **Usage:** วิธีการใช้ข้อมูลในแต่ละ Test Case (เช่น ใช้แล้วหมดไป หรือใช้ซ้ำได้)
- **Cleanup:** การลบหรือ Reset ข้อมูลหลังเทสเสร็จ เพื่อไม่ให้ข้อมูลขยะค้างในระบบ (โดยเฉพาะตอนทำ Automation)

---

## 📊 4. Test Data Categories (ประเภทข้อมูลที่ต้องเตรียม)

เราควรแบ่งประเภทข้อมูลตามวัตถุประสงค์:

|**ประเภทข้อมูล**|**คำอธิบาย**|**ตัวอย่าง**|
|---|---|---|
|**Standard / Valid**|ข้อมูลปกติที่ใช้งานได้ทั่วไป (Happy Path)|User สิทธิ์ปกติที่มีเงินในบัญชี|
|**Invalid / Error**|ข้อมูลที่ผิดกติกา เพื่อเทส Error Message|รหัสผ่านที่สั้นเกินไป, เลขบัญชีที่ไม่มีอยู่จริง|
|**Boundary**|ข้อมูลที่อยู่ตรงขอบเขตตาม Logic|ยอดโอนเงิน 50,000 บาท (สูงสุดพอดี)|
|**Large Scale**|ข้อมูลปริมาณมากเพื่อเทส Performance|ไฟล์ขนาด 5GB, ข้อมูล 100,000 รายการ|

---

## ⚠️ 5. Common Challenges (ปัญหาที่มักพบ)

- **Data Dependency:** ข้อมูลถูกใช้งานไปแล้ว ทำให้เทสซ้ำไม่ได้ (เช่น Voucher ที่ใช้ได้ครั้งเดียว)
- **Stale Data:** ข้อมูลเก่าเกินไปหรือสถานะเปลี่ยนไปแล้ว (เช่น สินค้าหมดสต็อก)
- **Shared Data:** QA หลายคนใช้ User เดียวกันเทสพร้อมกันจนค่าเพี้ยน

---

## 🔗 ความเชื่อมโยงไปไฟล์อื่น

- นำแนวทางนี้ไปออกแบบค่าตัวเลขจริง -> **[[05. Test Data Design]]**
- ระบุชื่อชุดข้อมูลในตารางเทส -> **[[02. Test Case Template]]** ในช่อง Test Data
- ตรวจสอบว่าข้อมูลรองรับทุกเงื่อนไขไหม -> **[[06. Traceability Matrix (RTM)]]**
