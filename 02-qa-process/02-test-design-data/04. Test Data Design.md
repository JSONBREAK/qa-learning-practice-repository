# 🎲 05. Test Data Design

**หมวด:** QA Process  
**Objective:** ออกแบบชุดข้อมูลสำหรับการทดสอบ (Test Data Sets) โดยเน้นความละเอียดของค่า Input เพื่อให้ครอบคลุมทั้งกรณีปกติ (Positive), กรณีผิดพลาด (Negative) และกรณีขอบเขต (Boundary)

---

## 🎯 Purpose

**Test Data Design** คือการนำกลยุทธ์จาก [[04. Test Data Strategy]] มาเปลี่ยนเป็น **"ข้อมูล"** ที่เราจะใช้กรอกในระบบจริงๆ  เป้าหมายของไฟล์นี้คือการระบุว่า "ต้องใช้ค่าอะไรบ้าง" เพื่อให้ครอบคลุม Logic ที่เราออกแบบไว้ใน Test Case Design

---

## 🧠 Test Data Design คืออะไร?
คือการ **ลงรายละเอียดของ Test Data**  
หลังจากที่มี Strategy แล้ว ว่าจะใช้ข้อมูลอะไรบ้าง

> Strategy = จะใช้อะไร  
> Design = ใช้อย่างไร / หน้าตาเป็นแบบไหน

---

## 🧩 หลักการออกแบบ Test Data ที่ดี

| Principle | Description |
|---------|-------------|
| **Reusable** | ใช้ซ้ำได้หลาย Test Case |
| **Readable** | อ่านแล้วเข้าใจทันที |
| **Isolated** | ไม่กระทบกันระหว่างเคส |
| **Traceable** | อ้างอิงกับ Test Case ได้ |

---

## 🧪 1. การออกแบบชุดข้อมูลตาม Logic (Design Approaches)

เราจะนำเทคนิคจาก [[01. Test Case Design|Test Case Design]] มาเปลี่ยนเป็นข้อมูลจริง ดังนี้:

### A. Boundary Value Data (ข้อมูลค่าขอบเขต)

เน้นตัวเลขที่ "ติดขอบ" ของเงื่อนไข เพราะเป็นจุดที่ Dev มักเขียนเครื่องหมายผิด (เช่น `<` แทนที่จะเป็น `<=`)

- **เงื่อนไข:** โอนเงินได้ 1 - 50,000 บาท
- **ชุดข้อมูล:** `0`, `1`, `49,999`, `50,000`, `50,001`
    

### B. Equivalence Partitioning Data (ข้อมูลตัวแทนกลุ่ม)

เลือกค่าที่เป็นตัวแทนของแต่ละกลุ่มข้อมูล

- **เงื่อนไข:** คะแนน 0-49 (Grade F), 50-100 (Grade P)
- **ชุดข้อมูล:** `25` (ตัวแทน F), `75` (ตัวแทน P)
    

### C. Specific Format Data (ข้อมูลตามรูปแบบ)

สำหรับฟิลด์ที่ต้องการ Format พิเศษ

- **Email:** `test@domain.com`, `test.sub@domain.co.th`
- **Password:** `Abc12345!` (Complex), `1234` (Simple/Short)

---

## 📊 2. Test Data Table (ตัวอย่างตารางออกแบบข้อมูล)

ในการทำงานจริง เรามักทำตารางสรุปข้อมูลไว้เพื่อให้เรียกใช้ง่าย:

| **Scenario ID** | **Field Name**     | **Input Value**     | **Data Type** | **Purpose**                   |
| --------------- | ------------------ | ------------------- | ------------- | ----------------------------- |
| **DS-01**       | `transfer_amount`  | `0.00`              | Negative      | Test zero amount validation   |
| **DS-02**       | `transfer_amount`  | `50,000.00`         | Positive      | Test maximum limit (Boundary) |
| **DS-03**       | `receiver_account` | `999-999-999`       | Negative      | Test non-exist account        |
| **DS-04**       | `remark`           | `A...Z (200 chars)` | Boundary      | Test max length of string     |

---

## 🎭 3. ประเภทของข้อมูลตามพฤติกรรม (Data Types)

1. **Valid Data (Happy Path):** ข้อมูลที่ถูกต้องสมบูรณ์ ระบบต้องประมวลผลผ่าน
2. **Invalid Data:** ข้อมูลผิดประเภท (เช่น ใส่ตัวอักษรในช่องยอดเงิน) ระบบต้องดักจับได้
3. **Missing Data (Null/Empty):** การไม่ใส่ข้อมูลในช่องที่บังคับ (Required Field)
4. **Special Characters:** การใช้ `!@#$%^&*()` หรือภาษาอื่นๆ (Emoji, ภาษาไทย/ญี่ปุ่น) เพื่อเทสการแสดงผลและการเก็บข้อมูล (Encoding)

---

## 🧹 4. Data States & Pre-requisites (สถานะของข้อมูล)

ข้อมูลบางอย่างต้องถูกเตรียมให้มี **"สถานะ"** ที่พร้อมก่อนเริ่มเทส:

- **User Account:** ต้องเป็น `Active`, `Locked`, หรือ `Expired`
- **Inventory:** สินค้าต้องมีจำนวน `In-stock` หรือ `Out-of-stock`
- **Balance:** บัญชีต้องมีเงิน `เพียงพอ` หรือ `ไม่พอ` สำหรับยอดที่จะโอน
    

---

## 🔗 ความเชื่อมโยงไปไฟล์อื่น

- นำข้อมูลที่ออกแบบไปใส่ใน -> **[[02. Test Case Template]]** (ช่อง Test Data)
- ตรวจสอบว่าข้อมูลชุดนี้ตอบโจทย์ Requirement ไหม -> **[[06. Traceability Matrix (RTM)]]**
- เป็นส่วนหนึ่งของขั้นตอนการเทสใน -> **[[03. Writing Test Steps]]**

---

