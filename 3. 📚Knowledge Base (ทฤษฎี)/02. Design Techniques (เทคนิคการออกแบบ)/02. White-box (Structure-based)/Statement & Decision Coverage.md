
# ⚪ 01. Statement & Decision Coverage

**หมวด:** White-box Techniques (Structure-based)

**Objective:** วัดความครอบคลุมของการทดสอบโดยดูจากโครงสร้างของ Code (Control Flow) เพื่อให้แน่ใจว่าตรรกะทุกอย่างถูกรันผ่านการเทส

---

## 1. Statement Coverage (รันครบทุกบรรทัด)

คือการวัดว่าชุดทดสอบ (Test Cases) ของเรา สั่งให้โปรแกรมรันผ่าน **"บรรทัดคำสั่ง" (Statements)** ทั้งหมดกี่เปอร์เซ็นต์

- **เป้าหมาย:** สั่งให้โค้ดทุกบรรทัดถูกประมวลผลอย่างน้อย 1 ครั้ง
    
- **สูตรคำนวณ:**
    
    $$\text{Statement Coverage} = \frac{\text{จำนวนบรรทัดที่ถูกรัน}}{\text{จำนวนบรรทัดทั้งหมด}} \times 100$$
    

> **ข้อจำกัด:** แม้จะรันครบ 100% แต่เราอาจจะพลาด "เงื่อนไขที่ทำให้เกิด False" ไปได้ (เช่น เคสที่ข้าม if ไปเลย)

---

## 2. Decision Coverage (รันครบทุกทางเลือก)

หรือเรียกอีกอย่างว่า **Branch Coverage** คือการวัดว่าชุดทดสอบของเราครอบคลุม "ทางเลือก" (True/False) ของเงื่อนไขทั้งหมดหรือไม่

- **เป้าหมาย:** ในทุกๆ จุดตัดสินใจ (เช่น `if`) ต้องมี Test Case ที่ทำให้เป็น **True** และ **False** อย่างน้อยอย่างละ 1 ครั้ง
    
- **สูตรคำนวณ:**
    
    $$\text{Decision Coverage} = \frac{\text{จำนวนทางเลือกที่ถูกรัน}}{\text{จำนวนทางเลือกทั้งหมด (True+False)}} \times 100$$
    

> **ความจริงที่ต้องรู้:** ถ้าคุณทำ **Decision Coverage ได้ 100%** คุณจะได้ **Statement Coverage 100% โดยอัตโนมัติ** 
> ...แต่การเก็บ **Statement Coverage** จนครบ 100% **ไม่ได้การันตี** ว่าจะเก็บ Decision ได้ครบทุกอย่าง"

---

## 🛠️ ตัวอย่างสถานการณ์ (Case Study)

**โค้ดตัวอย่าง:**

JavaScript

```
if (userAge >= 18) {
    print("Welcome to the club");  // บรรทัดที่ 1
}
print("Finish");                   // บรรทัดที่ 2
```

### Scenario A: ใช้ 1 Test Case (Age = 20)

- **ทางที่เดิน:** `if` เป็น **True** -> รันบรรทัดที่ 1 -> รันบรรทัดที่ 2
    
- **Statement Coverage:** **100%** (เพราะรันครบทุกบรรทัด)
    
- **Decision Coverage:** **50%** (เพราะยังไม่ได้ลองเคสที่ `if` เป็น False)
    

### Scenario B: เพิ่มอีก 1 Test Case (Age = 15)

- **ทางที่เดิน:** `if` เป็น **False** -> ข้ามบรรทัดที่ 1 -> รันบรรทัดที่ 2
    
- **ผลลัพธ์:** เมื่อรวบกับ Scenario A จะทำให้เราเทสครบทั้งทาง True และ False
    
- **Decision Coverage:** **100%**
    

---

## 📊 สรุปความแตกต่าง

|**หัวข้อ**|**Statement Coverage**|**Decision Coverage**|
|---|---|---|
|**สิ่งที่สนใจ**|บรรทัดคำสั่ง (Code lines)|เงื่อนไขและการตัดสินใจ (Branches)|
|**ความแข็งแกร่ง**|ต่ำกว่า (เจอบั๊กน้อยกว่า)|สูงกว่า (ครอบคลุม Logic มากกว่า)|
|**เป้าหมาย ISTQB**|รันผ่านทุกโหนด (Node)|รันผ่านทุกเส้นเชื่อม (Edge)|

---

## 💡 บันทึกช่วยจำ (Self-Note)

- ในฐานะ QA เราอาจจะไม่ได้เขียน Code เองทั้งหมด แต่เราควรรู้จักตัวเลขนี้เพื่อถาม Dev ว่า **"Unit Test ของคุณมี Decision Coverage ถึง 80-90% หรือยัง?"**
    
- **Decision Coverage** สำคัญมากในระบบที่มี Logic ซับซ้อน เพราะบั๊กมักจะซ่อนอยู่ในเงื่อนไขที่เรา "ลืมเทส" (เช่น ทางที่เป็น False ของ if)
    

---

