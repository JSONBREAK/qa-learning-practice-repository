### 🚀 ภาพรวมของ API สำหรับ QA

---

## API Testing คืออะไร (ในมุม QA)

> API = จุดที่ Frontend คุยกับ Backend

โดยไม่สนใจความสวยของ UI
สนแค่ ความถูกต้องของข้อมูล
ระบบรับ/ส่งข้อมูลถูกต้องตามสัญญา (Contract) ไหม

💡 ฐานของ Automation

---

#### 1. HTTP Methods (กริยาที่ใช้สั่งงาน)

QA ต้องจำ 4 ตัวหลักนี้ให้ขึ้นใจ:

|Method|ใช้ทำอะไร|ตัวอย่าง|
|---|---|---|
|GET|ดึงข้อมูล|ดูรายการผู้ใช้|
|POST|สร้างข้อมูล|สมัครสมาชิก|
|PUT|แก้ข้อมูลทั้งก้อน|แก้โปรไฟล์|
|PATCH|แก้บาง field|เปลี่ยนเบอร์|
|DELETE|ลบข้อมูล|ลบบัญชี|
### Structure ของ API Request
ทุก API Request มี 4 ส่วน

1. URL
```
					 https://api.example.com/users/123
```

2. Headers
```
						 Content-Type: application/json
						 Authorization: Bearer xxxxx
```

3. Body (เฉพาะ POST / PUT / PATCH)
JSON
```
{
"email": "test@mail.com",
"password": "123456"
}
```

4. Method: GET / POST / PUT / DELETE

---

#### 2. Status Codes (คำตอบจาก Server)

เวลาเรายิง API เราต้องดูว่ามันสำเร็จไหมจากเลขพวกนี้:

| Code | ความหมาย     | QA ควรคิด           |                            |
| ---- | ------------ | ------------------- | -------------------------- |
| 200  | Success      | ข้อมูลถูกต้องไหม    | ใช้กับ GET, PUT            |
| 201  | Created      | สร้างจริงไหม        | ใช้กับ POST                |
| 400  | Bad Request  | Validate ดีไหม      | เช่น ลืมกรอกรหัสผ่าน       |
| 401  | Unauthorized | Auth ทำงานไหม       | ลืม Login หรือไม่มีสิทธิ์  |
| 403  | Forbidden    | Permission ถูกไหม   |                            |
| 404  | Not Found    | Resource หายจริงไหม | หาหน้าหรือข้อมูลนั้นไม่เจอ |
| 500  | Internal Server Error | ❌ บั๊กฝั่ง Backend  | Bug ของ Developer          |

---

#### 3. เริ่มใช้ Postman

สิ่งที่ QA ต้องทำเป็น:
- สร้าง Request
- ส่ง Method ถูก
- อ่าน Response เป็น
- จับบั๊กจาก Response

- **URL/Endpoint:** ที่อยู่ของ API (เช่น `https://api.jSONBREAK.com/v1/users`)
- **Headers:** ข้อมูลเสริม (เช่น `Content-Type: application/json` หรือ Token สำหรับ Login)
- **Body:** ข้อมูลที่เราส่งไป (มักจะเป็นรูปแบบ JSON)

#### **ตัวอย่าง** Request  (GET)
**สิ่งที่ต้องเช็ก**: Status code ถูกไหม, Response เป็น JSON ไหม, Field ครบไหม, Type ถูกไหม

`{`
`"id": 1,`
`"name": "John",`
`"email": "john@mail.com"`
`}`

**สิ่งที่ต้องถาม**: id เป็น number?, email format ถูกไหม, field หาย = บั๊กไหม?


---

เวอร์ชัน Technical QA (API + SQL):
นี่คือสิ่งที่ต้องนึกภาพเวลาเขียนหรือรัน Test Case

|**ส่วนประกอบของ Test Case**|**สิ่งที่ QA ต้องทำจริง (Technical Action)**|
|---|---|
|**Input Data**|เตรียม **JSON Body** สำหรับยิง API (เช่น `{"name": "Surachok", "pos": "QA"}`)|
|**Test Steps (API)**|ยิงคำสั่ง **POST** ไปที่ URL `/api/v1/employees` ผ่าน Postman|
|**Expected Result 1 (API)**|Postman ต้องแสดง **Status Code: 201 Created** และ Response Body ต้องมี ID พนักงานตอบกลับมา|
|**Expected Result 2 (SQL)**|เมื่อใช้ **SQL** `SELECT * FROM employees WHERE name = 'Surachok'` ต้องเจอข้อมูลที่เพิ่งเพิ่มเข้าไป|

 **💡 ก่อนหน้านี้คือเรา Test จะรู้แค่ว่า testcase นี้พังอย่างเดียว  แต่ Postman จะบอกเราว่าพังที่จุดไหน**

---

🛠️ แผนการเรียน

1. **API ([[Postman]]):** เรียนรู้วิธียิง Request และการเช็ค Response (Status code & JSON Body)
2. **[[SQL]]:** เรียนรู้วิธีตรวจสอบว่า API ที่ยิงไปนั้น บันทึกข้อมูลลงฐานข้อมูลถูกต้องจริงไหม
3. **[[2. 🛠️Technical Stack (Hard Skills)/Automation (Playwright & Selenium)/Automation Strategy/#to-do Playwright]]:** เรียนรู้วิธีทำ Automate ที่หน้า UI เพื่อส่งข้อมูลผ่าน API อีกทีหนึ่ง


