## 🏗️ 1. หน้าตาและปุ่มที่ต้องรู้จัก (The Interface)

- **Request Builder:** ส่วนกลางที่ใช้เลือก Method (GET, POST, etc.) และใส่ URL
    
- **Params Tab:** สำหรับใส่ Query Parameters (ค่าที่อยู่หลังเครื่องหมาย `?` ใน URL)
    
- **Authorization Tab:** การระบุสิทธิ์ (เช่น Bearer Token, API Key)
    
- **Headers Tab:** กำหนดค่า Meta Data (เช่น `Content-Type: application/json`)
    
- **Body Tab:** ที่สำหรับใส่ข้อมูลที่จะส่งไป (มักใช้ `raw` > `JSON`)
    
- **Pre-request Script:** เขียน Code ให้ทำงาน "ก่อน" ยิง API (เช่น สร้างค่า Random)
    
- **Tests Tab:** เขียน Code ตรวจสอบผลลัพธ์ "หลัง" ยิง API เสร็จ
    

---

## ⚙️ 2. การตั้งค่าที่สำคัญ (Essential Settings)

- **SSL Certificate Verification:** แนะนำให้ **"OFF"** หากต้องเทสในระบบ Internal (Self-signed SSL) มิฉะนั้นจะยิง API ไม่ผ่าน
    
- **Language Detection:** ตั้งเป็น JSON เพื่อให้ Postman จัด Format ให้อ่านง่าย
    
- **Auto-save requests:** เปิดไว้เพื่อป้องกันงานหายเมื่อเปลี่ยน Request ไปมา
    

---

## 🌍 3. Variables & Environments (ระบบตัวแปร)

ส่วนนี้คือ "หัวใจ" ของการประหยัดเวลา:

- **Global Variables:** ค่าที่ใช้ได้กับทุกโปรเจกต์
    
- **Environment Variables:** ค่าที่แยกตามสภาพแวดล้อม (เช่น `URL_STAGING` vs `URL_PROD`)
    
- **Collection Variables:** ค่าที่ใช้ร่วมกันเฉพาะในโฟลเดอร์นั้น ๆ
    
- **วิธีเรียกใช้:** ใช้ปีกกาคู่เสมอ เช่น `{{base_url}}` หรือ `{{username}}`
    

---

## 🧪 4. Postman Console (ที่พึ่งยามยาก)

- **Shortcut:** `Ctrl + Alt + C` (Windows) หรือ `Cmd + Option + C` (Mac)
    
- **หน้าที่:** ใช้ดู "ไส้ใน" ของสิ่งที่ส่งไปและตอบกลับมา (Raw Request/Response)
    
- **QA Use Case:** เมื่อยิงแล้วได้ Error 500 เราจะก๊อปปี้ Log ใน Console นี้ไปให้ Dev ดู
    

---

## 🏃 5. Collection Runner (รันอัตโนมัติ)

- **คืออะไร:** การสั่งให้ Postman ยิง API ทุกตัวในโฟลเดอร์ต่อกันตามลำดับ
    
- **Delay:** การตั้งเวลารอระหว่าง Request (ป้องกันระบบล่มจากการรันเร็วเกินไป)
    
- **Data File:** การดึงข้อมูลจากไฟล์ `.csv` หรือ `.json` มาวนลูปเทสหลาย ๆ ชุดข้อมูล (Data Driven Testing)
    

---

## 📁 6. การจัดการความสะอาด (Organization)

- **Folders:** แยกตาม Feature หรือ User Story
    
- **Naming Convention:** ตั้งชื่อ Request ให้สื่อสาร เช่น `[POST] Register - Success` หรือ `[POST] Register - Fail (Email Duplicate)`
    
- **Documentation:** การเขียนอธิบายไว้ในช่อง Description ว่า API ตัวนี้มีเงื่อนไขอะไรบ้าง

---
