
**Objective:** เรียนรู้การเชื่อมโยงข้อมูลจากหลายตาราง (Join) และการใช้ฟังก์ชันการคำนวณ (Aggregates) เพื่อตรวจสอบความถูกต้องของข้อมูลในระดับที่ซับซ้อนขึ้น

---

## 🧩 1. ทำไมต้อง Join? (Data Relational)

ในระบบฐานข้อมูลที่ดี เขาจะไม่เก็บทุกอย่างไว้ในที่เดียวเพื่อลดความซ้ำซ้อน:

- **ตาราง `orders`:** เก็บ `order_id`, `amount`, `user_id`
    
- **ตาราง `users`:** เก็บ `user_id`, `first_name`, `email`
    

**โจทย์ของ QA:** "จงเช็คว่าออเดอร์หมายเลข ORD-99 เป็นของลูกค้าชื่ออะไร และอีเมลอะไร?" -> นี่คือหน้าที่ของ **JOIN**

---

## 🤝 2. ประเภทของ JOIN ที่ QA ต้องใช้บ่อย

### A. INNER JOIN (เอาเฉพาะที่ "ตรงกัน" ทั้งสองฝั่ง)

ใช้ตรวจสอบข้อมูลที่มีความสัมพันธ์กันสมบูรณ์

SQL

```
SELECT orders.order_id, users.first_name, orders.amount
FROM orders
INNER JOIN users ON orders.user_id = users.user_id;
```

> **QA Tip:** ถ้าใช้ Inner Join แล้วข้อมูลหายไป แสดงว่ามี `user_id` ในตาราง orders ที่ไม่มีตัวตนอยู่ในตาราง users (เจอ Bug Data Integrity!)

### B. LEFT JOIN (เอาฝั่งซ้ายเป็นหลัก ฝั่งขวาไม่มีไม่เป็นไร)

ใช้ตรวจสอบข้อมูลที่ "อาจจะ" มีหรือไม่มีข้อมูลเสริมก็ได้

SQL

```
SELECT users.first_name, orders.order_id
FROM users
LEFT JOIN orders ON users.user_id = orders.user_id;
```

> **QA Tip:** ใช้หาว่า "User คนไหนบ้างที่ยังไม่เคยสั่งซื้อสินค้าเลย" (ค่า `order_id` จะเป็น `NULL`)

---

## 🧮 3. Aggregate Functions (การคำนวณสรุปผล)

ใช้ตรวจสอบว่าระบบคำนวณตัวเลขหลังบ้านถูกต้องไหม (เช่น ยอดรวมตะกร้าสินค้า)

- **COUNT():** นับจำนวนแถว (เช่น นับว่า User นี้มีกี่ออเดอร์)
    
- **SUM():** รวมผลลัพธ์ (เช่น รวมยอดเงินทั้งหมด)
    
- **AVG():** หาค่าเฉลี่ย
    
- **MIN() / MAX():** หาค่าต่ำสุด/สูงสุด
    

**ตัวอย่างการใช้ GROUP BY (จัดกลุ่มเพื่อคำนวณ):**

SQL

```
-- เช็คว่าแต่ละ User มียอดซื้อรวมเท่าไหร่
SELECT user_id, SUM(amount) 
FROM orders 
GROUP BY user_id;
```

---

## 🔍 4. การกรองข้อมูลหลังการคำนวณ (HAVING)

ถ้าอยากกรองข้อมูล "หลังจาก" ที่เรา GROUP BY ไปแล้ว เราจะใช้ `WHERE` ไม่ได้ ต้องใช้ `HAVING`

**ตัวอย่าง:** "หา User ID ที่มียอดสั่งซื้อรวมมากกว่า 10,000 บาท"

SQL

```
SELECT user_id, SUM(amount)
FROM orders
GROUP BY user_id
HAVING SUM(amount) > 10000;
```

---

## 💡 Practical QA Scenarios: SQL Joins

- **ตรวจสอบการแสดงผลหน้า UI:** หน้าเว็บโชว์ชื่อลูกค้าถูกไหม? (ยิง Query Join ไปเช็คเทียบกับ UI)
    
- **ตรวจสอบ Report/Dashboard:** กราฟยอดขายรวมในระบบตรงกับ `SUM(amount)` ใน DB หรือไม่?
    
- **ตรวจสอบความสะอาดของข้อมูล:** มีออเดอร์ไหนที่ไม่มีเจ้าของ (User ID ผิด) หรือไม่?

---

**Next Step:** ตอนนี้มี "เครื่องมือ" ครบแล้ว ทั้งพื้นฐานและการ Join 
ต่อไปจะต้องเอา SQL ไปใช้ "วางแผนการเทส" ว่าใน 1 ฟีเจอร์ เราควรเปิด DB เช็คจุดไหนบ้าง

👉 [[Data Verification Strategy]]