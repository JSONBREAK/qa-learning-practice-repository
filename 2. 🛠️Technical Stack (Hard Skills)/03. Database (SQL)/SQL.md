
# 🗄️SQL (Structured Query Language)

**นิยาม:** คือ "ภาษาที่ใช้คุยกับฐานข้อมูล" เพื่อจัดการข้อมูลที่เก็บอยู่ในแผ่นดิสก์ (เปรียบเหมือน **ตู้เก็บเอกสาร**)

- **หน้าที่ของ QA:** ตรวจสอบว่า "ข้อมูลที่ถูกเก็บ" หรือ "ถูกดึงออกมา" ถูกต้องแม่นยำจริงไหม
- สิ่งที่ต้องเช็ค (Test Points):
	- **Data Integrity:** ข้อมูลที่บันทึกผ่านหน้าเว็บหรือ API ลงไปในตาราง (Table) ถูกต้องไหม (เช่น ชื่อสะกดถูกไหม, วันที่บันทึกตรงไหม)
	- **Data Relationship:** ข้อมูลในตาราง A เชื่อมกับตาราง B ถูกต้องไหม (Foreign Key)
	- **Database Constraints:** ระบบยอมให้บันทึกข้อมูลที่ผิดเงื่อนไขไหม (เช่น ลืมใส่อีเมลแล้วยอมให้ผ่านไหม)
- **เครื่องมือที่ใช้:** DBeaver, MySQL Workbench, pgAdmin

---

## 🔄 3. ตารางเปรียบเทียบ API vs SQL

|**หัวข้อเปรียบเทียบ**|**API (Postman)**|**SQL (Database)**|
|---|---|---|
|**บทบาท**|เป็นทางผ่านของข้อมูล (Interface)|เป็นที่เก็บข้อมูลถาวร (Storage)|
|**ตำแหน่งในระบบ**|อยู่ตรงกลาง (Middleware / Backend)|อยู่ชั้นในสุด (Data Layer)|
|**ภาษาที่ใช้**|JSON, XML, HTML|SQL (SELECT, INSERT, UPDATE)|
|**สิ่งที่ QA ใช้ตรวจ**|ตรวจสอบ "กระบวนการ" รับ-ส่ง|ตรวจสอบ "ความถูกต้อง" ของข้อมูลดิบ|
|**ตัวอย่างการเทส**|ยิง API สมัครสมาชิกแล้วดูว่าได้เลข 201 ไหม|เขียน SQL ไปดูในตาราง users ว่ามีชื่อเราโผล่มาไหม|

---

# 💡 Big Note 

QA ต้องใช้ทั้งคู่ร่วมกันอย่างไร? (E2E Testing Flow)

**Step 1 (UI/API):** 
- คุณทำ Automate (Playwright) กดปุ่ม "โอนเงิน" 500 บาท
**Step 2 (API Check):** 
- คุณตรวจสอบผ่าน Postman ว่า API ส่งค่ากลับมาเป็น `Success` และเลข Transaction ถูกต้อง
**Step 3 (SQL Check):** 
- เขียน SQL ไป Query ที่ตาราง `Accounts` เพื่อดูว่าเงินในบัญชี **"ลดลง 500 บาทจริงๆ"** หรือไม่

**สรุปสั้นๆ:** > * **API** คือการตรวจ "หน้าด่าน" ว่าคุยกันรู้เรื่องไหม
- **SQL** คือการตรวจ "หลังบ้าน" ว่าบันทึกของจริงถูกต้องไหม
