
---
## 🌐 API Fundamentals


## API คืออะไร
- API คือช่องทางให้ระบบ 2 ระบบคุยกันผ่าน HTTP
- เราเทสเพื่อยืนยันว่า **Request ถูก** และ **Response ถูก**

---
## 🏛️ 1. HTTP Request Structure

เวลาเรายิง API 1 ครั้ง จะประกอบด้วย 4 ส่วนสำคัญ:

1️⃣ **URL / Endpoint:** ที่อยู่ของข้อมูล (เช่น `https://api.store.com/v1/users`)

2️⃣ **HTTP Method (Verbs):** บอกว่า "จะทำอะไร"

- `GET` : ขอดูข้อมูล (Read)
    
- `POST` : สร้างข้อมูลใหม่ (Create)
    
- `PUT` : แก้ไขข้อมูลทั้งหมด (Update/Replace)
    
- `PATCH` : แก้ไขข้อมูลบางส่วน (Partial Update)
    
- `DELETE` : ลบข้อมูล (Delete)

3️⃣ **Headers:** ข้อมูลส่วนเสริม (เช่น `Content-Type: application/json` หรือ `Authorization: Token...`)

4️⃣ **Body (Payload):** ข้อมูลที่เราส่งไป (มักใช้กับ POST, PUT, PATCH) ในรูปแบบ **JSON**

---

## 📬 2. HTTP Response Structure

 Status Code ที่เจอบ่อย (เมื่อ Server ตอบกลับมา)
 
- **200 OK**
- **201 Created**
- **400 Bad Request**
- **401 Unauthorized**
- **403 Forbidden**
- **404 Not Found**
- **500 Internal Server Error**

---

## 📦 3. JSON Basics (ภาษามาตรฐานของ API)

JSON ย่อมาจาก _JavaScript Object Notation_ โครงสร้างจะเป็นคู่ของ **Key : Value**

```
{
  "user_id": 101,
  "user_name": "QA_Master",
  "is_active": true,
  "roles": ["Tester", "Admin"]
}
```

## JSON เบื้องต้น
- Object `{}` / Array `[]`
- Data types: string, number, boolean, null

## Checklist เทส API (แบบสั้น)
- [ ] Status Code ตรงที่คาด
- [ ] Response Schema ถูก
- [ ] Error Message ถูกในเคสผิด
- [ ] Response Time ไม่ช้าเกิน
- [ ] Data ถูกต้องตามเงื่อนไข

---

## 🔗 อ่านต่อ

- วิธีการใช้เครื่องมือจริง -> **[[Postman Guide]]**
    
- การนำ API มาใช้ร่วมกับงาน QA -> **[[03. API & Postman]]**
