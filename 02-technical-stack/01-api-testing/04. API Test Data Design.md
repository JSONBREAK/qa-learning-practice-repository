(เจาะลึกการเตรียมข้อมูล JSON สำหรับเคสยากๆ)

ออกแบบชุดข้อมูล (JSON Payload) ให้ครอบคลุมทั้ง Positive, Negative และ Edge Cases เพื่อค้นหาจุดบกพร่องที่ซ่อนอยู่หลังบ้าน

---

## 1. การออกแบบ Positive Data (ข้อมูลที่ควรจะผ่าน)

เป้าหมายคือการยืนยันว่าระบบทำงานได้ตาม Spec ที่ตกลงกันไว้

- **Minimum Valid Data:** ส่งเฉพาะ Field ที่บังคับ (Mandatory Fields) เท่านั้น
    
- **Full Valid Data:** ส่งครบทุก Field ทั้งที่บังคับและไม่บังคับ (Optional Fields)
    
- **Data Type Matching:** ตรวจสอบว่าส่ง String เป็น String, Number เป็น Number
    

---

## 2. การออกแบบ Negative Data (ข้อมูลที่ต้องโดนดัก)

ตรงนี้คือจุดที่ QA จะหาบั๊กได้เยอะที่สุด โดยใช้เทคนิคที่เราเรียนมา (EP/BVA):

- **Missing Mandatory Fields:** ลองลบ Key ที่สำคัญออก (เช่น ลบ `password` ออกจาก JSON) -> ระบบต้องตอบ `400 Bad Request`
    
- **Invalid Data Types:** * ส่งตัวอักษรในช่องที่รอรับตัวเลข เช่น `"age": "twenty"`
    
    - ส่งตัวเลขในช่องที่รอรับ Boolean เช่น `"is_active": 1`
        
- **Boundary Values:**
    
    - ส่งรหัสผ่านสั้นกว่า/ยาวกว่าที่กำหนด
        
    - ส่งยอดโอนเงินเป็น 0 หรือติดลบ
        
- **Format Violation:** ส่ง Email ที่ไม่มี `@` หรือส่งวันที่ผิดรูปแบบ (เช่น `31/02/2024`)
    

---

## 3. Edge Cases (กรณีสุดโต่ง)

ข้อมูลประเภทนี้มักจะทำให้ระบบ Crash หรือเกิด Security Issue:

- **Null Values:** ส่งค่าเป็น `null` ในฟิลด์ที่โปรแกรมเมอร์ไม่ได้เขียนดักไว้
    
- **Empty String:** ส่งค่าเป็น `""` (String ว่าง)
    
- **Special Characters:** ใส่ Script หรือสัญลักษณ์แปลกๆ เพื่อเช็ค SQL Injection หรือปัญหาการแสดงผล (เช่น `!@#$%^&*()_+` หรือ `<script>alert(1)</script>`)
    
- **Very Large Payload:** ส่งข้อความยาวมากๆ ในช่องที่ควรจะเป็นข้อความสั้นๆ เพื่อดูว่าระบบ Handle Buffer ยังไง
    

---

## 📊 ตัวอย่างตาราง Test Data สำหรับ API Register

|**Case ID**|**Scenario**|**Field: username**|**Field: age**|**Expected Status**|
|---|---|---|---|---|
|TC-01|Success (Min)|"qa_user"|20|201 Created|
|TC-02|Missing Field|_(ลบ key นี้ออก)_|20|400 Bad Request|
|TC-03|Invalid Type|"qa_user"|"twenty"|400 Bad Request|
|TC-04|Boundary (Min)|"qa" (2 chars)|20|400 (ถ้ากฎคือ 3+)|
|TC-05|Null Value|null|20|400 Bad Request|

---

## 🛠️ การจัดการ Data ใน Postman

เพื่อให้การเทสข้อมูลหลายๆ ชุดสะดวกขึ้น เราจะใช้ **Dynamic Variables** ของ Postman:

- `{{$randomFirstName}}` : สุ่มชื่อ
    
- `{{$randomEmail}}` : สุ่มอีเมลใหม่ทุกครั้งที่กด Send (ป้องกัน Error อีเมลซ้ำ)
    
- `{{$randomInt}}` : สุ่มตัวเลข

---
