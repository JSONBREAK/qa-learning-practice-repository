
เรียนรู้การใช้งาน Postman เพื่อส่ง Request, บริหารจัดการชุดทดสอบ (Collections), 
และการใช้ Environment Variables เพื่อให้การเทส API รวดเร็วและเป็นระบบ

---

## 🏗️ 1. หน้าตาและปุ่มที่ต้องรู้จัก (The Interface)

- **Request Builder:** ส่วนกลางที่ใช้เลือก Method (GET, POST, etc.) และใส่ URL
    
- **Params Tab:** สำหรับใส่ Query Parameters (ค่าที่อยู่หลังเครื่องหมาย `?` ใน URL)
    
- **Authorization Tab:** การระบุสิทธิ์ (เช่น Bearer Token, API Key)
    
- **Headers Tab:** กำหนดค่า Meta Data (เช่น `Content-Type: application/json`)
    
- **Body Tab:** ที่สำหรับใส่ข้อมูลที่จะส่งไป (มักใช้ `raw` > `JSON`)
    
- **Pre-request Script:** เขียน Code ให้ทำงาน "ก่อน" ยิง API (เช่น สร้างค่า Random)
    
- **Tests Tab:** เขียน Code ตรวจสอบผลลัพธ์ "หลัง" ยิง API เสร็จ
    

---

## 🌐 2. Environment Variables (ตัวแปรสำคัญ)

**ห้ามเขียน URL ตัวเต็มลงในทุก Request!** ให้ใช้ตัวแปรแทน เพื่อให้สลับไปเทสบน **Staging** หรือ **Production** ได้ทันทีโดยไม่ต้องแก้ Code

- **Global Variables:** ค่าที่ใช้ได้กับทุกโปรเจกต์
    
- **Environment Variables:** ค่าที่แยกตามสภาพแวดล้อม (เช่น `URL_STAGING` vs `URL_PROD`)
    
- **Collection Variables:** ค่าที่ใช้ร่วมกันเฉพาะในโฟลเดอร์นั้น ๆ
    
- **วิธีเรียกใช้:** ใช้ปีกกาคู่เสมอ เช่น `{{base_url}}` หรือ `{{username}}`
    

---

## 🧪 3. การเขียน Test Scripts เบื้องต้น

Postman มี Tab ที่ชื่อว่า **"Tests"** เอาไว้เขียน Code (JavaScript) สั้นๆ เพื่อให้ระบบตรวจผลลัพธ์ให้เราอัตโนมัติ

### ตัวอย่าง Snippets ที่ใช้บ่อย:

```
// 1. เช็คว่า Status Code ต้องเป็น 200
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

// 2. เช็คว่า Response ที่ได้มามีคำว่า "success" หรือไม่
pm.test("Response body contains success message", function () {
    pm.expect(pm.response.text()).to.include("success");
});

// 3. เช็คค่าใน JSON Response
pm.test("Verify User ID", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData.user_id).to.eql(101);
});
```

---

## 📋 4. Postman Workflow สำหรับ QA

1. **Create Collection:** ตั้งชื่อตามชื่อโมดูลที่เทส
2. **Setup Environment:** กำหนด URL และ Token ที่ต้องใช้
3. **Add Requests:** ใส่ Method (GET, POST, etc.) และ Body (JSON)
4. **Write Tests:** เขียน Script ตรวจสอบ Status Code และ Data
5. **Run Collection:** ใช้ **Collection Runner** เพื่อยิงทุก Request ต่อกันอัตโนมัติ (Automation เบื้องต้น)

---

## 💡 Pro-Tips สำหรับคนทำ Postman

- **Console is your friend:** กด `Ctrl + Alt + C` เพื่อเปิด Console ดู Log เวลา API พัง จะเห็นละเอียดว่าเราส่งอะไรไป และ Server ตอบอะไรกลับมา
    
- **Import/Export:** สามารถ Export Collection เป็นไฟล์ `.json` เพื่อส่งให้เพื่อนในทีม หรือเอาไปใส่ในโปรเจกต์ Automation (Newman) ได้
    
- **Save Response:** ถ้าเจอ Bug ให้กด **Save Response** ไว้เป็นหลักฐานเพื่อแนบใน Bug Report

---

## 🛠️ API & Postman (The Integration)

ในการเทสจริง API มักจะไม่ทำงานตัวเดียวโดดๆ แต่ต้องส่งค่าต่อกัน เช่น _Login -> ได้ Token -> เอา Token ไปโอนเงิน_

**วิธีทำใน Postman:** ใช้ `pm.environment.set` ใน Tab **Tests** เพื่อเก็บค่าจาก API ตัวแรกไปใช้ในตัวถัดไป

```
// ใน API Login: เก็บ Token ไว้ในตัวแปร environment
var jsonData = pm.response.json();
pm.environment.set("access_token", jsonData.token);
```

จากนั้นใน API ตัวถัดไป ให้ใส่ `{{access_token}}` ในส่วนของ Header (Authorization)

---

## 🧪 2. API Test Scenarios (ต้องเทสอะไรบ้าง?)

เวลาเราเทส API ด้วย Postman ให้มอง 3 มุมมองหลัก:

### A. Functional Testing (ทำงานถูกไหม)

- **Status Code:** ตรงตามที่ตกลงกันไว้ไหม (200, 201, 400, etc.)
- **Response Body:** Key และ Value ครบไหม (เช่น ต้องมี `user_id`, `email`)
- **Data Type:** ข้อมูลที่ส่งมาเป็น String, Number, หรือ Boolean ตาม Spec หรือไม่
    

### B. Logical & Business Testing (Logic ถูกไหม)

- **Negative Case:** ถ้าถอนเงินมากกว่าที่มี ต้องได้ Error Message ที่ถูกต้อง (ไม่ใช่ 500 Server Error)
- **Permission:** ถ้า User A พยายามไปดูข้อมูลของ User B ระบบต้องตอบ `403 Forbidden`
    

### C. Performance & Security (เบื้องต้น)

- **Response Time:** API ตัวนี้ช้าเกินไปไหม (เช่น ไม่ควรเกิน 500ms)
- **Sensitive Data:** มีรหัสผ่าน (Password) หลุดออกมาใน Response Body แบบไม่เข้ารหัสหรือไม่
    

---

## 🏃 3. Collection Runner (Run All at Once)

เมื่อเราเขียน Test Scripts ไว้ในทุก Request แล้ว เราไม่ต้องกดปุ่ม Send ทีละตัว:

1. เลือก Collection ที่ต้องการ
2. กดปุ่ม **Run**
3. เลือกจำนวนรอบ (Iterations) และ Delay
4. Postman จะสรุปผลให้ว่า **Pass/Fail** กี่เคส

---

## 📊 4. ตัวอย่างตารางวางแผน API Test Case

|**Scenario**|**Method**|**Endpoint**|**Expected Status**|**Expected Response**|
|---|---|---|---|---|
|Login Success|POST|`/login`|200|มี `token` ส่งกลับมา|
|Login Fail (Wrong Pass)|POST|`/login`|401|Message: `Invalid Credentials`|
|Get User Profile|GET|`/profile`|200|`email` ต้องตรงกับที่ Login|
|Access Without Token|GET|`/profile`|401|Message: `Unauthorized`|

---

## 🔗 ความเชื่อมโยงถัดไป

- เมื่อมั่นใจว่า API ทำงานถูกแล้ว -> ไปตรวจข้อมูลในฐานข้อมูลต่อที่ **[[02. Intermediate SQL & Data Joins]]**
    
- ข้อมูล API ที่ได้ จะนำไปใช้เปรียบเทียบกับหน้าจอใน UI Testing