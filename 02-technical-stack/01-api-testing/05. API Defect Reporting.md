(วิธีแคป API Log หรือส่อง Error 500 ไปแจ้ง Dev)

รู้วิธีรวบรวมหลักฐานเชิงเทคนิคเมื่อ API ทำงานผิดปกติ เพื่อเขียน Bug Report ที่มีคุณภาพสูง (High-Quality Defect)

---

## 1. อะไรคือ "หลักฐาน" ที่ Dev ต้องการจาก API?

เวลา API พัง Dev ไม่ได้อยากได้แค่ภาพหน้าจอขาวๆ หรือ Error 500 แต่เขาต้องการข้อมูล 3 ส่วนนี้เพื่อไปตรวจสอบ Log หลังบ้าน:

1. **Request Details:** เราส่งอะไรไป? (Method, URL, Headers, Body)
    
2. **Response Details:** ระบบตอบอะไรกลับมา? (Status Code, Error Message, Response Time)
    
3. **Trace ID / Correlation ID:** (ถ้ามี) คือ ID พิเศษที่ใช้ตามหา Log ในระบบหลังบ้านที่ซับซ้อน
    

---

## 2. วิธีดึงหลักฐานจาก Postman Console 🖥️

ปุ่ม `Send` อย่างเดียวไม่พอ QA มือโปรต้องเปิด **Console** (`Ctrl + Alt + C`) เพราะมันจะโชว์ **Raw Data** ทั้งหมด:

- **Copy as Curl:** ใน Postman เราสามารถคลิกขวาที่ Request แล้วเลือก `Copy` -> `Copy as cURL` เพื่อให้ Dev เอาคำสั่งนี้ไปรันในเครื่องตัวเองเพื่อจำลองบั๊กได้ทันที (อันนี้ Dev รักมาก!)
    
- **Pretty vs Raw:** ในหน้า Response ให้ก๊อปปี้แบบ `Pretty` (ที่จัดบรรทัดแล้ว) จะอ่านง่ายกว่า `Raw`
    

---

## 3. การเขียน Summary สำหรับ API Bug

การตั้งชื่อหัวข้อบั๊ก API ควรระบุ **[Method] + [Endpoint] + [Problem]**

- ❌ **Bad:** ยิง API สมัครสมาชิกไม่ได้
    
- ✅ **Good:** `[POST] /api/v1/register - ระบบตอบ 500 Internal Server Error เมื่อส่งค่า age เป็น null`
    

---

## 🐞 ตัวอย่าง API Bug Report (Template)


```
- Summary: 
  [POST] /v1/transfer - ระบบอนุญาตให้โอนเงินสำเร็จเมื่อยอดเงินในบัญชีไม่เพียงพอ

- Environment: 
  Staging API (v.1.2.0)

- Severity: Critical

- Steps to Reproduce:
  1. ยิง API /v1/transfer ด้วย Method POST
  2. Payload (Body):
     {
       "sender_id": "QA_001",
       "amount": 1000,   // ยอดโอน
       "balance": 500    // ยอดเงินที่มีจริง
     }

- Actual Result:
  Status: 200 OK
  Body: { "status": "success", "message": "Transfer completed" }

- Expected Result:
  Status: 400 Bad Request
  Body: { "status": "fail", "message": "Insufficient balance" }

- Evidence:
  [Link to Postman Console Log] หรือ [cURL Command]
```

---

## 💡 Pro-Tips: แยกแยะอาการเบื้องต้น (First Diagnostic)

- **Error 400:** เช็ค Payload ของเราก่อน (เราอาจจะพิมพ์ Key ผิด หรือส่ง Data Type ผิดเอง)
    
- **Error 401/403:** เช็ค Token ว่าหมดอายุหรือยัง หรือลืมใส่ Header หรือเปล่า
    
- **Error 500:** อันนี้ "ของแท้" ส่วนใหญ่เป็นบั๊กที่ Code หลังบ้าน ให้ก๊อปปี้ Response Body ไปส่งให้ Dev ทันที

