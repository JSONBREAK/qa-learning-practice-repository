
**Objective:** รวมสูตรสำเร็จคำสั่ง SQL ที่ใช้บ่อยในการทำ Data Verification เพื่อความรวดเร็วและแม่นยำในการทดสอบ

---

## 1. 🔍 การตรวจสอบข้อมูลล่าสุด (Latest Entry)

ใช้ตรวจสอบทันทีหลังจากทำรายการหน้า UI/API เสร็จ เพื่อดูว่าข้อมูลเข้า DB ไหม

SQL

```
-- ดูข้อมูล 5 แถวล่าสุด เรียงตามเวลาที่สร้าง
SELECT * FROM table_name 
ORDER BY created_at DESC 
LIMIT 5;

-- ดูข้อมูลล่าสุดตัวเดียวที่เป็นของเรา (ระบุ User)
SELECT * FROM orders 
WHERE user_id = 'QA_USER_01' 
ORDER BY created_at DESC 
LIMIT 1;
```

---

## 2. 👥 การตรวจสอบข้อมูลซ้ำ (Duplicate Detection)

ใช้เช็ค Bug ประเภทข้อมูลซ้ำซ้อนที่ระบบไม่ควรยอม (เช่น สมัครอีเมลเดิมซ้ำได้)

SQL

```
-- ตรวจหา Email ที่มีมากกว่า 1 รายการในตาราง
SELECT email, COUNT(*)
FROM users
GROUP BY email
HAVING COUNT(*) > 1;
```

---

## 3. 🕳️ การตรวจสอบค่าว่าง (Null & Empty Check)

ใช้เช็คว่ามีฟิลด์ Mandatory (บังคับกรอก) ตัวไหนที่หลุดรอดไปเป็นค่าว่างไหม

SQL

```
-- เช็คทั้งที่เป็น NULL และเป็น String ว่าง
SELECT * FROM users 
WHERE phone_number IS NULL OR phone_number = '';
```

---

## 4. 🔗 การตรวจสอบความสัมพันธ์ (Table Joins)

ใช้ดูว่าข้อมูลที่กระจายอยู่หลายตาราง เชื่อมโยงกันถูกต้องไหม

SQL

```
-- ดูชื่อลูกค้า (จากตาราง users) พร้อมกับหมายเลขคำสั่งซื้อ (จากตาราง orders)
SELECT u.first_name, u.email, o.order_id, o.status
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id
WHERE o.order_id = 'ORD-12345';
```

---

## 5. 💰 การตรวจสอบยอดรวมและการคำนวณ (Aggregates)

ใช้เช็คว่า Logic การคำนวณหลังบ้านตรงกับหน้าจอไหม

SQL

```
-- เช็คยอดรวมเงินในตารางออเดอร์ของ User คนนี้
SELECT user_id, SUM(total_price) AS total_spent, COUNT(order_id) AS order_count
FROM orders
WHERE user_id = 'QA_USER_01'
GROUP BY user_id;
```

---

## 6. 📅 การตรวจสอบช่วงเวลา (Date & Time)

ใช้ตรวจสอบข้อมูลย้อนหลัง หรือรายการที่เกิดขึ้นในวันนั้นๆ

SQL

```
-- ค้นหารายการที่เกิดขึ้นในวันนี้ (ปรับ Date Format ตาม DB ที่ใช้)
SELECT * FROM transactions 
WHERE created_at >= '2024-03-20 00:00:00' 
  AND created_at <= '2024-03-20 23:59:59';
```

---

## 7. 🛡️ การตรวจสอบความปลอดภัย (Security Check)

ใช้เช็คว่าข้อมูลลับถูกปกปิดดีพอไหม

SQL

```
-- เช็คว่า Password ต้องไม่อ่านออกเป็นคำตรงๆ (ต้องเป็น Hash)
SELECT username, password FROM users 
WHERE username = 'test_user';

-- เช็คหา User ที่มีสิทธิ์เป็น Admin ทั้งหมด
SELECT * FROM users WHERE role = 'ADMIN';
```

---

## 💡 Tips สำหรับ QA

- **Aliasing (AS):** ใช้ตั้งชื่อเล่นให้ตาราง (เช่น `FROM users AS u`) จะช่วยให้เขียน Query ยาวๆ ได้ง่ายขึ้นมาก
    
- **Explain:** ถ้า Query ช้ามาก ให้ลองใส่คำว่า `EXPLAIN` ข้างหน้าคำสั่ง เพื่อดูว่า Index ทำงานไหม (สำหรับ Advanced QA)
    
- **Format:** เวลาแนบหลักฐานใน Bug Report ให้ก๊อปปี้ผลลัพธ์จาก SQL Client ในรูปแบบ **CSV** หรือ **Markdown Table** จะทำให้ Dev อ่านง่ายขึ้น