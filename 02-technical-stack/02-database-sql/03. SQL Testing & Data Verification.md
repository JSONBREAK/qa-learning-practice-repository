**Objective:** เรียนรู้กลยุทธ์การตรวจสอบความถูกต้องของข้อมูลในฐานข้อมูล (Database) เพื่อยืนยันว่า Business Logic ทำงานถูกต้องตั้งแต่หน้าบ้านจนถึงหลังบ้าน

---

## 1. กลยุทธ์การเทสด้วยหลักการ CRUD

ในการเทสฟีเจอร์ส่วนใหญ่ ข้อมูลมักจะหนีไม่พ้น 4 การกระทำนี้ (Create, Read, Update, Delete)

### 🟢 Create (สร้างข้อมูล)

- **สิ่งที่ต้องเช็ค:** ข้อมูลใหม่ต้องเข้าจริง, ID ต้องรันต่อกัน (Auto Increment), ค่า Default ต้องถูกต้อง
    
- **SQL Strategy:** `SELECT * FROM table ORDER BY created_at DESC LIMIT 1;`
    

### 🔵 Read (อ่านข้อมูล)

- **สิ่งที่ต้องเช็ค:** ข้อมูลที่โชว์บนหน้าเว็บ/แอป ต้องตรงกับใน DB (เช็คทุก Field)
    
- **SQL Strategy:** `SELECT * FROM table WHERE id = 'TARGET_ID';`
    

### 🟡 Update (แก้ไขข้อมูล)

- **สิ่งที่ต้องเช็ค:** แก้ไขเฉพาะ Field ที่เราแก้ (Field อื่นต้องเหมือนเดิม), `updated_at` ต้องเปลี่ยนเป็นเวลาปัจจุบัน
    
- **SQL Strategy:** เทียบข้อมูลระหว่าง "ก่อนแก้" และ "หลังแก้" ด้วย `SELECT`
    

### 🔴 Delete (ลบข้อมูล)

- **สิ่งที่ต้องเช็ค:**
    
    - **Hard Delete:** ข้อมูลต้องหายไปจากตารางจริง
        
    - **Soft Delete:** ข้อมูลยังอยู่แต่สถานะเปลี่ยน (เช่น `is_deleted = 1` หรือ `status = 'INACTIVE'`)
        
- **SQL Strategy:** `SELECT * FROM table WHERE id = 'TARGET_ID';`
    

---

## 2. การตรวจสอบความสมบูรณ์ของข้อมูล (Data Integrity)

### A. Data Truncation (ข้อมูลถูกตัด)

- **จุดตรวจสอบ:** ถ้าหน้าเว็บยอมให้กรอกชื่อยาว 255 ตัว แต่ DB เก็บได้แค่ 50 ตัว ข้อมูลจะถูกตัดทิ้ง
    
- **วิธีเทส:** ลองกรอกข้อมูลยาวๆ (Boundary Value) แล้ว Query ดูว่าใน DB เก็บครบทุกตัวอักษรไหม
    

### B. NULL vs Empty String

- **จุดตรวจสอบ:** ฟิลด์ที่ไม่บังคับกรอก ระบบควรเก็บเป็น `NULL` หรือ `''` (String ว่าง) ตามที่ตกลงกันไว้
    
- **SQL Strategy:** `SELECT * FROM table WHERE column_name IS NULL;`
    

### C. Constraint Violation (กฎของฐานข้อมูล)

- **จุดตรวจสอบ:** ลองทำสิ่งที่ผิดกฎ เช่น สมัครสมาชิกด้วยอีเมลซ้ำ ระบบต้อง Error และใน DB ต้องไม่มี Row ใหม่เกิดขึ้น
    

---

## 3. Business Logic Verification (เช็คตรรกะทางธุรกิจ)

บางครั้ง API บอกว่าสำเร็จ (200 OK) แต่เลขใน DB อาจจะคำนวณผิด

- **ยอดเงิน/แต้ม:** หลังโอนเงิน แต้มของผู้โอนต้องลดลง และแต้มผู้รับต้องเพิ่มขึ้นตามจำนวนที่โอนจริง
    
- **สถานะสินค้า:** เมื่อกดชำระเงินสำเร็จ สถานะสินค้าในตารางสต็อกต้องลดลง 1 ชิ้น
    
- **SQL Strategy:** ใช้การ `SUM()` หรือ `COUNT()` เพื่อเช็คยอดรวมหลังทำรายการ
    

---

## 4. Security Verification (ความปลอดภัยพื้นฐาน)

- **Sensitive Data:** รหัสผ่าน (Password) หรือเลขบัตรเครดิต **ห้ามเก็บเป็นข้อความปกติ (Plain Text)** ต้องมีการเข้ารหัส (Hash/Encrypt) ที่คนอ่านไม่ออก
    
- **Unauthorized Update:** ลองใช้ User A แอบแก้ข้อมูล User B แล้วเช็คใน DB ว่าข้อมูล User B ยังปลอดภัยดีอยู่ไหม