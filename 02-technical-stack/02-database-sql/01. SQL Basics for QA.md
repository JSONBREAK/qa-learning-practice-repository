**Objective:** เข้าใจโครงสร้างฐานข้อมูลเบื้องต้น และฝึกใช้คำสั่ง SQL พื้นฐานเพื่อดึงข้อมูลมาตรวจสอบความถูกต้อง (Data Verification)

---

## 🏛️ 1. Relational Database 101

ก่อนจะพิมพ์คำสั่ง เราต้องเข้าใจว่าข้อมูลถูกเก็บอย่างไร:

- **Table:** ตารางเก็บข้อมูล (เช่น ตาราง `Users`, `Orders`)
    
- **Column:** หัวข้อของข้อมูล (เช่น `id`, `email`, `status`)
    
- **Row / Record:** ข้อมูล 1 แถว (เช่น ข้อมูลของลูกค้า 1 คน)
    
- **Primary Key (PK):** Column ที่ห้ามซ้ำ ใช้ระบุตัวตน (เช่น `user_id`)
    

---

## 🔍 2. การดึงข้อมูล (The "SELECT" Power)

หัวใจของ QA คือการดูว่าข้อมูลที่บันทึกลงไป "ถูกต้อง" หรือไม่

### A. ดึงข้อมูลทั้งหมด

SQL

```
SELECT * FROM users;
```

_(ใช้บ่อยเวลาอยากดูภาพรวมว่ามี Data อะไรอยู่ในตารางบ้าง)_

### B. ดึงเฉพาะคอลัมน์ที่สนใจ (เพื่อให้อ่านง่ายขึ้น)

SQL

```
SELECT first_name, last_name, email FROM users;
```

---

## 🎯 3. การกรองข้อมูล (The "WHERE" Clause)

QA มักต้องระบุเคสที่จะเทส ดังนั้นคำสั่ง `WHERE` คือส่วนที่สำคัญที่สุด

### ค้นหาแบบระบุค่าตรงๆ

SQL

```
SELECT * FROM orders WHERE order_id = 'ORD-001';
```

### ค้นหาแบบเงื่อนไขตัวเลข

SQL

```
SELECT * FROM products WHERE price > 500;
```

### ค้นหาหลายเงื่อนไข (AND / OR)

SQL

```
SELECT * FROM users WHERE status = 'active' AND country = 'Thailand';
```

### ค้นหาบางส่วน (Pattern Matching)

SQL

```
SELECT * FROM users WHERE email LIKE '%@gmail.com'; 
-- % คือการบอกว่า "อะไรก็ได้"
```

---

## 🧹 4. การจัดระเบียบข้อมูล (Sorting & Limiting)

เวลาเทส เรามักอยากเห็น "ข้อมูลล่าสุด" ที่เราเพิ่งกรอกไป

### เรียงลำดับข้อมูล

SQL

```
SELECT * FROM users ORDER BY created_at DESC; 
-- DESC = ล่าสุดไปเก่าสุด / ASC = เก่าไปล่าสุด
```

### ดึงเฉพาะข้อมูลล่าสุด 1 ตัว

SQL

```
SELECT * FROM orders ORDER BY order_date DESC LIMIT 1;
-- ช่วยให้ไม่ต้องเลื่อนหาข้อมูลท่ามกลาง Data เป็นล้านแถว
```

---

## 💡 QA Insight: ทำไม SQL ถึงสำคัญ?

บางครั้งหน้าจอ (UI) แสดงผลว่า **"Success"** แต่เบื้องหลังอาจเกิดความผิดปกติ เช่น:

1. **Truncated Data:** ชื่อยาวเกินไป แล้ว DB ตัดชื่อทิ้ง (เช็คได้ใน DB เท่านั้น)
    
2. **Default Values:** ค่าเริ่มต้นบางอย่างที่ไม่ได้แสดงใน UI ถูกเซ็ตมาผิด
    
3. **Ghost Data:** ลบข้อมูลใน UI แล้วหายไป แต่ใน DB ยังอยู่ (Hard Delete vs Soft Delete)

---

**Next Step:** เมื่อดึงข้อมูลตารางเดียวคล่องแล้ว จะมาเรียนรู้วิธีการ "เชื่อมตาราง" 
เพราะข้อมูลส่วนใหญ่มักจะกระจายอยู่หลายที่ (เช่น ข้อมูลสั่งซื้อ อยู่คนละตารางกับชื่อลูกค้า)

👉 [[02. Intermediate SQL & Data Joins]]
