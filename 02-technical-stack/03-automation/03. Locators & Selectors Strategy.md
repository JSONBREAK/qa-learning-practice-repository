หัวใจของการเขียน Automation  สำคัญมาก เพราะถ้าเราสั่งผิด(หาปุ่มไม่เจอ) เทสก็จะพังทันที 
ไฟล์นี้คือการหา **Locators** ให้แม่นยำและทนทาน (Robust) ต่อการเปลี่ยนแปลงของ UI 

---

# 🎯 03. Locators & Selectors Strategy

**สถานะ:** 🛠️ In-Progress

**Objective:** เรียนรู้วิธีระบุตำแหน่ง Element บนหน้าเว็บ (ปุ่ม, ช่องกรอก, ข้อความ) อย่างมีประสิทธิภาพ เพื่อให้ Script ทำงานได้อย่างเสถียรที่สุด

---

## 1. Locators คืออะไร?

Locators คือ "พิกัด" ที่เราบอกให้ Automation Tool (Playwright/Selenium) รู้ว่าต้องไปจัดการกับส่วนไหนของหน้าจอ เปรียบเสมือนที่อยู่บ้านของ Element นั้นๆ ในโครงสร้าง **DOM (Document Object Model)**

---

## 2. ลำดับความน่าเชื่อถือของ Locators (Priority Table)

ไม่ใช่ทุกวิธีจะดีเท่ากัน QA มือโปรจะเลือกใช้ตามลำดับความ "นิ่ง" ของมัน:

|**Priority**|**Locator Type**|**ตัวอย่าง**|**คำแนะนำ**|
|---|---|---|---|
|**1 (ดีสุด)**|**Data Attributes**|`[data-testid="login-btn"]`|**ดีที่สุด!** เพราะ Dev สร้างไว้เพื่อเทสโดยเฉพาะ ไม่เปลี่ยนตาม UI|
|**2**|**ID**|`#login-button`|ดีมาก เพราะต้องมีหนึ่งเดียวในหน้าจอ (แต่บางครั้ง Dev ลืมใส่)|
|**3**|**Role & Text**|`getByRole('button', { name: 'Submit' })`|**Playwright แนะนำ!** เพราะอิงตามประสบการณ์ผู้ใช้จริง (Accessibility)|
|**4**|**CSS Selector**|`.btn-primary > span`|ปานกลาง ถ้า UI เปลี่ยน (เช่น เปลี่ยนสี/คลาส) Script อาจพัง|
|**5 (ควรเลี่ยง)**|**XPath (Absolute)**|`/html/body/div[1]/form/button`|**ห้ามใช้!** ถ้ามีอะไรขยับนิดเดียวพังทันที|

---

## 3. Playwright Recommended Locators (Modern Way)

Playwright มีวิธีหาตำแหน่งที่ฉลาดกว่า Selenium โดยเน้นสิ่งที่ User เห็นจริงๆ:

- **getByRole():** หาตามประเภท เช่น button, checkbox, heading
    
- **getByText():** หาจากข้อความที่ปรากฏบนจอ
    
- **getByLabel():** หาจากชื่อป้ายกำกับของช่องกรอกข้อมูล
    
- **getByPlaceholder():** หาจากข้อความจางๆ ในช่องกรอก
    

JavaScript

```
// ตัวอย่างการเขียนใน Playwright
await page.getByRole('button', { name: 'เข้าสู่ระบบ' }).click();
```

---

## 4. กลยุทธ์การเขียน XPath & CSS Selector (เมื่อไม่มี ID)

บางครั้งเราเลี่ยงไม่ได้ต้องเขียนเอง:

- **CSS Selector:** เน้นสั้น กระชับ เช่น `input.login-input`
    
- **XPath (Relative):** ใช้เมื่อต้องหาความสัมพันธ์ที่ซับซ้อน เช่น "หาปุ่มลบ ที่อยู่ข้างๆ ชื่อ User A"
    
    - `//user-row[contains(., "User A")]//button[@id="delete"]`
        

---

## 💡 Tips สำหรับ QA: เลือก Locator ยังไงไม่ให้พังบ่อย?

1. **ขอร้อง Dev:** ให้ช่วยใส่ `data-testid` หรือ `data-qa` ให้ในจุดที่สำคัญที่สุด
    
2. **เลี่ยงการอิงลำดับ (Index):** อย่าใช้ `div[5]` เพราะถ้าวันหน้ามีปุ่มเพิ่มมาใหม่ Index จะเลื่อนทันที
    
3. **ใช้บางส่วนของคำ (Contains):** ถ้า ID มันดูเหมือนจะสุ่ม (เช่น `btn_12345`) ให้ใช้ `[id*="btn_"]` แทน

---

