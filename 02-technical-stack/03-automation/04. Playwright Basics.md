# 🎭 04. Playwright Basics (จดสรุป)

**Objective:** ติดตั้ง, รู้โครงสร้าง และสั่งบอทให้ทำงานพื้นฐานได้ (Go, Fill, Click, Verify)

---

## 1. การติดตั้ง (Setup)

ต้องมี **Node.js** ในเครื่องก่อน จากนั้นเปิด Terminal แล้วลุย:

Bash

```
npm init playwright@latest
```

- **เลือก:** TypeScript (พิมพ์ Code ง่าย มีตัวช่วยสะกด)
    
- **โหลดเบราว์เซอร์:** ตอบ Yes ให้หมด
    

---

## 2. โครงสร้างไฟล์ (ที่ต้องสนใจจริงๆ)

- `tests/` : ที่เก็บไฟล์เทสของเรา (ต้องลงท้ายด้วย `.spec.ts`)
    
- `playwright.config.ts` : ไฟล์เซ็ตค่าหลัก (รันที่ไหน, รันกี่ Browser, Retry กี่รอบ)
    
- `test-results/` : ถ้าเทสพัง รูป Screenshot หรือวิดีโอจะมาโผล่ที่นี่
    

---

## 3. สรุปคำสั่งที่ "ต้องใช้" บ่อยที่สุด

|**คำสั่งสั่งบอท**|**หน้าที่**|
|---|---|
|`page.goto('/')`|เปิดหน้าเว็บ (อิงตาม base_url ใน config)|
|`page.fill('selector', 'text')`|พิมพ์ข้อมูล (ลบของเก่าแล้วพิมพ์ใหม่)|
|`page.click('selector')`|คลิกปุ่ม/ลิงก์|
|`page.check('selector')`|ติ๊กถูก (Checkbox/Radio)|
|`page.waitForTimeout(ms)`|**(ห้ามใช้ถ้าไม่จำเป็น)** ให้บอทหลับรอ|

|**คำสั่งตรวจสอบ (Assertion)**|**หน้าที่**|
|---|---|
|`await expect(el).toBeVisible()`|เช็คว่า "มองเห็น" ของบนหน้าจอไหม|
|`await expect(el).toContainText('..')`|เช็คว่ามีคำที่ต้องการอยู่ในนั้นไหม|
|`await expect(page).toHaveURL(/.*checkout/)`|เช็คว่าหน้าเปลี่ยนไป URL ที่ถูกต้องหรือยัง|

---

## 4. เขียน Script แรก (ตัวอย่างจดไว้ดู Template)

ไฟล์: `tests/first-test.spec.ts`

TypeScript

```
import { test, expect } from '@playwright/test';

test('Basic Flow Test', async ({ page }) => {
    await page.goto('https://saucedemo.com'); // เว็บตัวอย่างสำหรับหัดเทส
    
    // พิมพ์และคลิก
    await page.locator('[data-test="username"]').fill('standard_user');
    await page.locator('[data-test="password"]').fill('secret_sauce');
    await page.locator('[data-test="login-button"]').click();
    
    // ตรวจสอบผล
    await expect(page).toHaveURL(/.*inventory.html/);
    await expect(page.locator('.title')).toContainText('Products');
});
```

---

## 5. วิธีรัน (Execution)

- **รันแบบ UI (เห็นทุก Step):** `npx playwright test --ui` _(แนะนำมากเวลาเขียนใหม่)_
    
- **รันแบบเบื้องหลัง (เร็ว):** `npx playwright test`
    
- **รันแค่ไฟล์เดียว:** `npx playwright test tests/login.spec.ts`
    

---

## 💡 บันทึกช่วยจำ (Self-Note)

- **อย่าลืม `await`:** ทุกคำสั่งที่สั่งหน้าจอต้องมี `await` นำหน้าเสมอ ไม่งั้นบอทจะงง
    
- **ใช้ Codegen เมื่อตัน:** นึกไม่ออกว่าต้องเขียนยังไงให้พิมพ์ `npx playwright codegen [url]` แล้วกดหน้าจอเอา แล้วค่อยก๊อปโค้ดมาแต่งต่อ
    

---

**Next Step:** ถ้าจดส่วนนี้เรียบร้อยแล้ว ต่อไปคือการทำ **Page Object Model (POM)** เพื่อแยก "ที่อยู่ปุ่ม" ออกจาก "ขั้นตอนเทส" จะได้ไม่พังง่ายเวลา Dev เปลี่ยนหน้าเว็บ